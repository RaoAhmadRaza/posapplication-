// Customer moved to lib/features/customers/ (its own CRM feature). Re-exported
// here so existing Sales/POS imports keep resolving unchanged.
export '../../../customers/domain/entities/customer.dart';
