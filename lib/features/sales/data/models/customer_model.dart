// CustomerModel moved to lib/features/customers/ (its own CRM feature).
// Re-exported so existing Sales/POS imports keep resolving unchanged.
export '../../../customers/data/models/customer_model.dart';
