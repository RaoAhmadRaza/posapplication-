import '../../../../core/design/widgets/app_states.dart';

// These states were promoted to the design system when suppliers needed the
// same shape. The repair names stay as aliases so its call sites are untouched.
typedef RepairEmptyState = AppEmptyState;
typedef RepairErrorState = AppErrorState;
